let quantidade = parseFloat(prompt('Qual a quantidade de números que você quer saber a média?'))

const num = parseInt(prompt(`Informe os números`))

while (num == quantidade) {
    alert(`Por favor, digite apenas 1, 2 ou 3`)
    metodoDePagamento = parseInt(prompt(mensagem))

    //AINDA EM CONSTRUÇÃO ----------